package com.sistema.fyda.mapper;

import org.springframework.beans.factory.annotation.Autowired;

import com.sistema.fyda.dto.PessoaDTO;
import com.sistema.fyda.model.Pessoa;

public class PessoaMapper {
	
	/*
	 * @Autowired Pessoa pessoa;
	 * 
	 * @Autowired PessoaDTO pessoaDTO;
	 * 
	 * public Pessoa getConverterPessoaDTOToEntity(PessoaDTO pessoaDTO) {
	 * 
	 * Pessoa pessoa = new Pessoa(); pessoa.setNome(pessoaDTO.getNome());
	 * pessoa.setCidade(pessoaDTO.getCidade());
	 * pessoa.setEndereco(pessoaDTO.getEndereco());
	 * pessoa.setCpf(pessoaDTO.getCpf()); pessoa.setIdade(pessoaDTO.getIdade());
	 * 
	 * return pessoa; }
	 * 
	 * public PessoaDTO getConverterPessoaToPessoaDTO(Pessoa pessoa) {
	 * 
	 * PessoaDTO pessoaDTO = new PessoaDTO(); pessoaDTO.setNome(pessoa.getNome());
	 * pessoaDTO.setCidade(pessoa.getCidade());
	 * pessoaDTO.setEndereco(pessoa.getEndereco());
	 * pessoaDTO.setCpf(pessoa.getCpf()); pessoaDTO.setIdade(pessoa.getIdade());
	 * 
	 * return pessoaDTO; }
	 * 
	 */
	
	

}
